<header class="header user">
    <div class="container">
        <nav class="nav">
            <a href="/" class="logo">
                <img src="/images/logo.png" alt="" />
            </a>
            <div class="hamburger-menu">
                <i class="fas fa-bars"></i>
                <i class="fas fa-times"></i>
            </div>
            <ul class="nav-list ">
                <li class="nav-item">
                    <a href="/" class="nav-link">Home</a>
                </li>
                <li class="nav-item">
                    <a href="#footer" class="nav-link">About</a>
                </li>
                {{#if googleuser}}
                <li class="nav-item">
                    <a href="/booking" class="nav-link">Bookings</a>
                </li>
                {{else if user.hotel}}
                <li class="nav-item">
                    <a href="/hotel/bookingdetails" class="nav-link">Bookings</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a href="#" class="nav-link">Bookings</a>
                </li>
                {{/if}}

                {{#if user}}
                <li class="nav-item">
                    <a href="/profile" class="nav-link userheader">
                        {{#if done}}
                        <img class="homephoto" src="/HOTEL/{{user._id}}.jpg " alt={{user.username}} />
                        <span class="homephotoname">{{user.username}}</span>
                        {{else}}
                        <span>{{user.username}}</span>
                        {{/if}}
                    </a>
                </li>
                {{else if googleuser}}
                <li class="nav-item">
                    <a href="/profile" class="nav-link userheader">
                        {{#if googleuser}}
                        <img class="homephoto" src=" {{googleuser.picture}}" alt={{googleuser.name}} />
                        <span class="homephotoname">{{googleuser.name}}</span>
                        {{else}}
                        <span>{{googleuser.name}}</span>
                        {{/if}}
                    </a>
                </li>
                {{else}}
                <li class=" nav-item">

                </li>
                {{/if}}
                {{#if user}}
                <li class="nav-item">
                    <a href="/logout" class="nav-link">Logout</a>
                </li>
                {{else if googleuser}}
                <li class="nav-item">
                    <a href="/logout" class="nav-link">Logout</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a href="/login" class="nav-link">Login</a>
                </li>

                {{/if}}



            </ul>
        </nav>
    </div>
</header>